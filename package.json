{
  "name": "WebDevSite",
  "private": true,
  "scripts": {
    "start": "bin/rails server -p 5000",
    "start:prod": "bin/rails server -p 80 -e production",
    "deploy": "RAILS_ENV=production bin/rails assets:precompile && gcloud app deploy"
  },
  "dependencies": {}
}
