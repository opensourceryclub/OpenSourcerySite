import React from "react"

describe("<Section />", () => {
    it("TODO write tests", () => {
        expect(1).toBe(1)
    })
})

describe("<SectionRow />", () => {
    it("TODO write tests", () => {
        expect(1).toBe(1)
    })
})

describe("<Column />", () => {
    it("TODO write tests", () => {
        expect(1).toBe(1)
    })
})
